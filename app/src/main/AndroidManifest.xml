<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="artyfartyparty.solo">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.AppCompat.Light">
        <activity
            android:name=".Controller.LoginActivity"
            android:label="Login"
            android:parentActivityName=".Controller.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controller.SignUpActivity"
            android:label="Sign Up"
            android:parentActivityName=".Controller.LoginActivity">
            <!-- Parents activity meta-data to support -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Controller.LoginActivity" />
        </activity>
        <activity
            android:name=".Controller.AllRidesActivity">
            android:label="All Rides"
            android:parentActivityName=".Controller.LoginActivity">
            <!-- Parents activity meta-data to support -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Controller.LoginActivity" />
        </activity>
        <activity
            android:name=".Controller.AddRideActivity">
            android:label="Add Ride"
            android:parentActivityName=".Controller.LoginActivity">
            <!-- Parents activity meta-data to support -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Controller.LoginActivity" />
        </activity>
    </application>

</manifest>